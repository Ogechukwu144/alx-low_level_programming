#include "main.h"

/**
 * _abs - print the absolute value of an integer
 * @m: parameter to be computed
 * Return: The absolute value
 */

int _abs(int m)
{
	if (m >= 0)
		return (m);
	else
		return (-m);
}
